# Raw pulls and per-run research are ephemeral and reference customer orders — never commit them.
data/
# Per-run outputs are regenerated; a stale board is a trap.
dist/
site/
node_modules/
.netlify/
